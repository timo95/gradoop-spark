package org.gradoop.spark.io.impl.csv

import org.gradoop.spark.io.api.DataSink
import org.gradoop.spark.model.api.graph.{GraphCollection, LogicalGraph}

class CsvDataSink extends DataSink {

  override def write(graphCollection: GraphCollection[_, _, _, _, _]): Unit = {

  }

  override def write(graphCollection: GraphCollection[_, _, _, _, _], overwrite: Boolean): Unit = {

  }

  override def write(logicalGraph: LogicalGraph[_, _, _, _, _]): Unit = {

  }

  override def write(logicalGraph: LogicalGraph[_, _, _, _, _], overwrite: Boolean): Unit = {

  }
}
